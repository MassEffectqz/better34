# TODO — план улучшений briefly

Список задач из аудита. Статусы: `[x]` — сделано, `[ ]` — запланировано.

## Баги и тех-долг

- [x] 1. Лимит скачивания 4 МБ делает загрузку видео невозможной — лимит
  зависит от типа файла (видео → 256 МБ), переменная `BRIEFLY_MAX_DOWNLOAD_MB`.
- [x] 2. `DeletePost` удаляет каталог целиком — удалять только файл поста,
  каталог — только если он опустел.
- [x] 3. `CleanDuplicates` недетерминирован — сортировать, предпочитать пути БД.
- [x] 4. gofmt чистый на 13 файлах, CRLF → LF, CI-проверка `gofmt -l`.
- [x] 5. Security-заголовки (nosniff, referrer, frame, permissions).
- [x] 6. `UpdateSettings` не даёт очистить поля — `*string` как у auto_download.
- [x] 7. `.env` никто не читает — загрузка в run.ps1 + Go, добавлен `.env.example`.
- [x] 8. Мёртвые файлы data/* (~50 МБ), `importLegacyJSON` не читает 25 МБ при старте.
- [x] 9. Покрытие тестами — интеграционный httptest-сценарий (ready/metrics,
  регистрация, settings partial-update, смена пароля, logout-others); QR/zip/recommend — отдельно.

## Бэкенд

- [x] 1. log/slog структурированный лог (в main) + `BRIEFLY_LOG_LEVEL`;
  полная миграция internal — отдельной задачей.
- [x] 2. Readiness-эндпоинт `/api/ready` (проверка БД).
- [x] 3. `/api/metrics` — лёгкая самодиагностика (очередь, SSE, кэши).
- [x] 4. Автобэкап БД: `VACUUM INTO data/backups/` раз в сутки + ротация
  (кол-во `BRIEFLY_DB_BACKUPS`, возраст `BRIEFLY_DB_BACKUP_DAYS`).
- [x] 5. Ретрай при 429/5xx в downloader.
- [x] 6. FTS5-поиск по тегам — отклонён: движок недоступен в pure-Go
  modernc.org/sqlite (только CGO-сборки). Локальный поиск уже идёт по
  нормализованной таблице tags с PK (tag, post_id) и покрывается тестами.

## Безопасность

- [x] 1. Смена пароля + отзыв остальных сессий.
- [x] 2. Rate-limit на QR-эндпоинты (без поломки poll).
- [x] 3. Права 0600 на файлы с ключами/хэшами.

## Фронтенд / PWA

- [x] 1. Тесты: auth, settings, offline-очередь, i18n.
- [x] 2. Background Sync для офлайн-очереди: enqueue регистрирует sync-тег
  'briefly-flush', sync-обработчик в sw.js выгружает очередь даже при закрытых
  вкладках (Chromium; Firefox/Safari — фолбэк 'online' + flush при загрузке
  страницы). Параллельная доставка страница/воркер сериализована Web Locks.
- [x] 3. Push-уведомления о завершении загрузок.
- [ ] 4. a11y: фокус-трапы, aria-live, видимый фокус во вьювере.
- [ ] 5. i18n: серверные ошибки локализованы; часть строк в статике.
- [ ] 6. Миниатюры видео (ffmpeg-кадр). Hover-плей НЕ сломается: он уже
  реализован отдельным скрытым `<video>` в карточке ленты, постер — статичный `<img>`.

## DevOps / CI

- [x] 1. CI: шаг `gofmt -l`.
- [x] 2. Docker multi-arch (amd64/arm64), фикс отсутствия checkout в docker-job.
- [x] 3. alpine 3.19 → 3.22.
- [x] 4. Пример docker-compose.yml.
- [x] 5. Пере-нормализация переносов строк (LF).

## Новые функции

- [ ] Дедуп по pHash при скачивании (сейчас только md5).
- [ ] Кадр видео для «похожих» — после ffmpeg-превью.
- [ ] Алиасы тегов (danbooru-style).
- [ ] Массовые действия: лайк/скрытие/коллекция для выделенного.